[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: requires running MySQL and Redis services
    unit: pure unit tests, no external dependencies
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
